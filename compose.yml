services:
  app:
    container_name: portfolio
    image: portfolio
    build:
      context: .
      dockerfile: deploy/Dockerfile
    restart: unless-stopped
    expose:
      - 80
    networks:
      - proxy

networks:
  proxy:
    external: true
