---
export interface Props {
  className: string;
  href: string;
}

const { href, className } = Astro.props
---

<a href={href} class={className} target="_blank" rel="noreferrer noopener">
  <slot />
</a>